@model recipe_in_home.Models.Post
@{
    ViewBag.Title = "Delete";
}
<h2>게시글 삭제</h2>

<p>다음 게시글을 삭제하시겠습니까?</p>

<div>
    <h4>게시글 정보</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            제목
        </dt>
        <dd class="col-sm-10">
            @Model.Title
        </dd>
        <dt class="col-sm-2">
            내용
        </dt>
        <dd class="col-sm-10">
            @Model.Content
        </dd>
        <dt class="col-sm-2">
            작성자
        </dt>
        <dd class="col-sm-10">
            @Model.member_id
        </dd>
    </dl>

    @using (Html.BeginForm("DeleteConfirmed", "Post", new { id = Model.member_id }, FormMethod.Post))
    {
        @Html.AntiForgeryToken()
        <div class="form-group">
            <input type="submit" value="삭제" class="btn btn-danger" />
            <a asp-action="Index" class="btn btn-default">취소</a>
        </div>
    }
</div>